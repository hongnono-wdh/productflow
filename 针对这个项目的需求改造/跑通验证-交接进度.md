# 交接进度：完整跑通 FocusFlow 真实项目（验证还原度新流程）

> **用途**：上下文将满，本文件是**跨对话交接**——下次对话读它即可无缝继续。
> **整理时间**：2026-07-03

---

## 0. 我们在做什么

**目标**：完整、认真地真跑一个真实项目 `FocusFlow`（从 ①→⑥/⑦），端到端验证「还原度提升方案」新流程在真实完整流程里是否顺；**跑通后再做 P3-1（自定义首图）需求**。
**用户要求**：过程中发现**不对的 / 可优化的 / 风险的**，随时**暂停问用户确认**，别闷头跑完。

---

## 1. 环境状态（已就绪，下次直接用）

- **生图 key**：`~/.config/openai/env` 已写（`OPENAI_API_KEY=sk-e9fb...` + `OPENAI_BASE_URL=https://api.gjs.ink`，600 权限）。**gpt-image-2 预检已通过**（真出图）。生图前先 `set -a; . ~/.config/openai/env; set +a` 加载。
- **pixelmatch**：已装（`--user --break-system-packages`）。`fidelity_diff.py` 现用 pixelmatch 引擎。
- **操作台 7717**：已重启到 **2.32.0**（新 server + dist）。start.sh 不重启已跑的进程——要重启得先 `lsof -ti :7717 | xargs kill` 再 `sh productflow/scripts/start.sh`。
- **web build**：已修复（之前缺 `marked` 导致 vite build 全挂、dist stale；已 `npm install` 修好）。
- **未修的预存问题**（非本方案范围，未擅改）：`web/src/components/SystemFlowCanvas.tsx` typecheck `any` 错（不挡 vite build）；`phase-3-hero.md` 第 1 行 `条镇` 乱码。

---

## 2. 当前项目 FocusFlow

- **位置**：`~/code/focusflow`（id `focusflow-app-5fef`），注册在 `~/.productflow`。
- **产品**：FocusFlow 极简待办 App 落地页，**PC 单页 waitlist（涉后端：邮箱收集 → 会跑到 ⑦）**。
- brief + `wizard.json`（platforms=["PC"], primary=PC, stylePrefs 极简/冷色/大留白）已设。
- **CLI 快捷函数**（下次直接用）：
  ```bash
  PF() { python3 /Users/weiyong/Documents/10/productflow/productflow/scripts/pf_state.py --dir ~/code/focusflow "$@"; }
  ```

### 进度
- ✅ **① 市场调研 done**：真实竞品调研（Todoist/Superlist/TickTick/Things）→ `artifacts/phase-1/replicate-notes.md`（信息架构 hero→功能三宫格→社会证明→Footer CTA；**风格方向 A：极简冷色 royal blue `#2563eb` + slate 文字 + 大留白**；核心矛盾：产品理念"少即是多"→落地页也要极简）。
- ⏳ **正在 ② 找参考**（下次从这里继续）：计划 = playwright 截 1–2 个真实待办 App 落地页（Todoist/Superlist）作风格参考（只学结构/风格，不盗图）→ `Read` 萃取 token → 锁 shadcn（全自动 `spec set-lib`）→ 生成组件目录 → `spec set-tokens` 写草案。
- ⬜ 未做：②(进行中) ③④⑤⑥⑦⑧。

### 完整跑法（剩余阶段计划）
- **②** 找参考 + `extract-lock`（萃取 token 草案 + 锁 shadcn + 组件目录）
- **③** 生首图（gpt-image-2，PC 1440x1080 纯 UI）+ `Read` 反萃取精确 token（R-③b）
- **④** 页面设计（生页面图 or 复用首图）+ `spec set-page` 组件映射 + 交互态
- **⑤** 功能数据（waitlist 邮箱表 ER + 数据态 `spec set-page --state`）
- **⑥** 前端实现：`spec check`+`spec compile` → 按组件映射写前端（用 token 变量）→ **起 dev server 本地真跑** → 环境确定性截图 → 三层闸门（我 Read 设计稿↔实现做裁判出 `fidelity-<页>.json` + `fidelity_diff` 差异图 + DOM 断言）→ `phase 6 --status done`（fidelity 硬闸：product 页需 verdict==pass）
- **⑦** 后端（waitlist 邮箱收集 API + 测试；**含还原度第 3 层 DOM 断言**，已在 phase-7 手册补齐）
- **⑧** 部署（可选，**需用户凭证 → 到时停下问用户**）
- **核心里程碑**：⑥ 前端在本地真跑起来（浏览器能开、表单能交互）。

---

## 3. 待办（用户已明确，流程走通后做）

- **P3-1 · ③ 首图自定义传入（上传/拖入，没传才生图）**：已记录进 `改造需求清单.md`（commit `85fbb74`），含初步方向（复用 `uploadRef` 机制：server `uploadHero` + `HeroPanel` 上传入口 + phase-3 分支）。**流程走通后做这个。**

## 4. 测试跑暴露的改进点（建议补手册，待用户确认，未做）

1. **token 命名**：agent（含我）萃取时易把语义词放 primitive 层（`color.primary`），`spec check` 会警告 → 建议 phase-2/3 手册加「primitive 用 `color.blue.500`、语义放 semantic 层」的明确示例。
2. **图标**：实现易用字符（▤◎⟳）代替设计稿线性图标 → 建议 ⑥ 手册引导用图标库（lucide/heroicons）。

## 5. 本次会话已完成的修复（已提交）

- `adc3f8a` fix(⑦)：phase-7 补还原度第 3 层 DOM 断言呼应（衔接遗漏——⑥手册说"涉后端并入⑦测试"但⑦手册原先没呼应）。
- `85fbb74` docs：记录 P3-1 需求。

---

## 6. 大背景（还原度方案本体，已全部落地）

还原度方案 v2 **全 5 期已实施**（`2.28.0→2.32.0`，dev 分支，**未 push**，~30 提交，211 测试绿）。文档：`还原度提升方案-全流程.md`（主纲）+ `还原度方案-A~E-*.md`（5 专题）+ `还原度实施-进度.md`（逐任务进度，全打勾）。详见 memory `project-fidelity-fullflow-plan`。

---

## 7. 下次对话怎么继续（给未来的我）

1. 读本文件 + memory `project-fidelity-fullflow-plan`。
2. 确认环境（`curl -s http://127.0.0.1:7717/api/version` 应 2.32.0；`~/.config/openai/env` 在；`python3 -c "from pixelmatch import pixelmatch"` 可 import）。若 7717 没跑，重启（见 §1）。
3. 用 §2 的 `PF()` 函数，从 **② 找参考**继续（`PF phase 2 --status active` → 找参考/萃取 token/锁库）。
4. 每阶段做完 `PF status` 看进度；遇不对/可优化/风险**停下问用户**。
5. 全部走通后，做 **P3-1**（§3）。

---

## 8. 最新进度（2026-07-05 本轮 loop 更新）——①→⑤ 全部完成，下次从 ⑥ 起

> 本轮把 FocusFlow 从 ①（其实上会话只写了文件没收尾）真正推进到 **⑤ 完成**。design-spec 脊椎已完整贯通 ②③④⑤。

### 已完成（真实状态，操作台可见）
- **① 市场调研 done**：补齐了上会话漏的收尾（注册 replicate-notes.md + 4 步 done + 起 spec 骨架 + phase done）。
- **② 找参考 done**：抓真实参考（onepagelove /tag/minimal 6 张 + dribbble 1 张，存 `artifacts/phase-2/refs/`），选定 opl-3+opl-5；extract-lock 萃取 21 个 token 草案（`artifacts/phase-2/tokens-draft.json`）+ 锁 **shadcn/ui+tailwind theme=slate** + 组件目录（`artifacts/phase-2/component-catalog.md`，6 组件）。风格 = **A 极简冷色 #2563eb**（用户拍板）。
- **③ 首图 done**：混合生 4 版（gpt-image-2，`artifacts/phase-3/heroes/hero-{1..4}-*.png`），**定稿 hero-1-gen-a.png**（完整 waitlist 架构，用户拍板）；反萃取补 **semantic 层 8 个 alias**（color.action.primary→blue.500 等），styleSummary 已落。
- **④ 页面设计 done**：单页 marketing，复用 hero-1 作 PC 稿（`artifacts/phase-4/landing-pc.png`，pg-509c13）；direction.md 五节定稿；组件映射（Badge/Input/Button/Card/Icon）+ 交互态落 spec。
- **⑤ 功能与数据 done**：waitlist 单模块/单表 subscribers/单接口 POST /api/subscribe；schema.sql（SQLite，sqlite3 验证过）；数据态；系统流程图 backend-flow；**栈选定 T3（Node+Hono+better-sqlite3，前端 Vite+React+Tailwind+shadcn+lucide-react）**（用户拍板）。产物 6 份全登记。

### design-spec 现状（`spec show` / compile 已验证可用）
componentLib.PC=shadcn/slate + catalog；tokens：primitive 21（color.blue.*/slate.*/font/space/radius/shadow）+ semantic 8（color.action.*/text.*/bg.*/border.*）；pages[pg-509c13] type=marketing + 7 组件映射 + 交互态(hero.cta/email/footer.cta 含 success/error)。`spec compile --platform PC` 出 tokens.css 正常。

### ⑥ 下次怎么起（核心里程碑：前端本地真跑）
1. 读 `phase-6-frontend.md`。`PF phase 6 --status active`。
2. **pre-flight**：`spec check` + `spec compile --platform PC --out <前端>/src/tokens.css`。
3. 按 **component-catalog.md 组件映射 + tokens.css 变量**写前端（Vite+React+Tailwind+shadcn+lucide），**别硬编码色值/用字符图标**。视觉照 hero-1（`artifacts/phase-3/heroes/hero-1-gen-a.png`）+ direction.md 五节。
4. 后端 Hono + better-sqlite3 跑 schema.sql，实现 POST /api/subscribe（去重 409）。
5. **起 dev server 本地真跑** → 环境确定性截图（playwright chromium **已装**，1440 视口、fonts.ready、禁动画）→ 三层闸门：Read 设计稿↔实现裁判出 `artifacts/phase-6/fidelity-<页>.json`(verdict==pass) + `fidelity_diff.py` 差异图 + DOM 断言 → 3 轮自纠 → `PF phase 6 --status done`（fidelity 硬闸：product/marketing 页需 verdict==pass）。
6. ⑦ 后端测试（含第 3 层 DOM 断言）；⑧ 部署需凭证·停下问。

### 本轮发现的「改造完善」素材（真实踩坑，建议进手册/工具改进 backlog）
1. **step/artifact 收尾易漏**（反复出现）：agent 跑阶段常只写产物文件，漏调 `pf_state` 注册产物/标 step done → 操作台看不到进展。本轮 ① 整个漏、search-refs/gen-heroes 漏 step done、⑤ artifact 路径误加 `.productflow/` 前缀致 artifacts:0。**建议**：phase done 时校验「所有 step 非 pending + 磁盘产物都已注册」，未注册就警告。
2. **`spec set-token` alias 写法无示例**：phase-3 只演示 `--value #hex`，没演示 alias（正确 = `--value "{color.blue.500}"`；`--ref` 是布尔 flag 不接值）→ 首次写失败。手册补 alias 示例。
3. **`artifact`/`spec set-tokens --file` 路径基准不一**：artifact 的 file 相对 `.productflow/`，但 `set-tokens --file` 相对 cwd（非项目目录）→ 易找不到。手册/CLI 统一或提示。
4. **生图 `--count N` 在 gjs.ink 中转只出 1 张**；`edit.py` 同 prompt 输出同名会覆盖 → 手册提醒中转 API count 行为 + 生成后即重命名/独立 out-dir。
5. **zsh 陷阱**：shell 函数 + for 循环 + `{...}` 花括号会触发 brace expansion/环境异常 → 编排 pf_state 用 python subprocess 最稳（已固化用法）。
6. phase-3 手册第 1 行 `条镇` 乱码（预存，仍未修）。

### task 跟踪
#6-#10 completed（①收尾+②③④⑤）；#11 ⑥（pending，下次主攻）；#12 ⑦；#13 ⑧(需凭证)；#14 P3-1；#15 手册改进点（可并入上面「改造完善素材」）。

---

## 9. 二段更新（2026-07-05 续跑）——⑥⑦ 完成，①→⑦ 全通 + 完整产品本地真跑

> 本轮从 ⑥ 一路推到 ⑦，FocusFlow **完整产品端到端真跑通**。核心目标（跑通验证还原度新流程）**已达成**。下次从 **P3-1 自定义首图** 或 **⑧ 部署**（需凭证）起。

### ⑥ 前端实现 done
- 栈：Vite+React+TS+Tailwind+**手写 shadcn 风格组件**+lucide-react（真图标库）。子代理构建，主线程裁判。
- 完整 waitlist 落地页（导航/hero/功能三宫格/社会证明/footerCTA），用 tokens.css 变量、lucide 图标、移动端汉堡菜单+SEO/og meta+a11y+表单6态。
- **三层闸门**：第1层 LLM 裁判 **verdict=pass**（我+子代理双裁判一致，差异全 minor：留白略舒展/avatar色块/字号微大）→ `fidelity-landing.json`；第2层 pixelmatch **4.9%** → `fidelity-landing-diff.png`；第3层 DOM 断言并入 ⑦。impl-check 硬闸通过。
- 本地真跑验证 HTTP 200。P6 artifacts:9（含 error/success 态截图）。

### ⑦ 后端实现·测试 done
- 栈：**Hono + @hono/node-server + better-sqlite3**，单端口 serve dist + /api（真实产物）。
- `POST /api/subscribe`：服务端校验(400)/UNIQUE去重(409)/成功(201)/**honeypot**防滥用；SQLite `subscribers` 表（`journal_mode=DELETE` 防 WAL 不落盘历史事故）。
- **四类测试全绿（主线程独立复跑确认，非口头）**：①单元6(vitest) ②E2E旅程3(playwright) ③第3层DOM断言2 ④持久化往返1(真SIGKILL重启读回)。test-report/api-docs/build-summary/docs(api.md/design.md/README) 齐。
- 系统流程图 waitlist/api 状态=done。

### 完整产品本地端到端验证（最终验证·已通过）
- 起完整产品：`8787` 单端口。真提交 `demo@focusflow.app`→**201落库**、重复→409、非法→400；DB subscribers 表确认落库。前端+API+SQLite+去重+校验全真实工作。

### 起服务命令（下次/演示用）
```bash
cd /Users/weiyong/code/focusflow
npm start            # build dist + Hono serve 前端+API on 8787（完整产品）
npm run server       # serve 已 build 的 dist（PORT/DB_PATH 可覆盖），DB=data/focusflow.db
npm run preview      # 仅前端预览 4173（无后端）
npm test / npm run test:e2e / npm run test:persist   # 四类测试
```
⚠️ 子代理起的 server 会随其进程结束被杀——要"本地真跑"验证得主线程 `nohup` 重起（已固化经验）。

### 本轮二段新踩的坑（补充「改造完善素材」）
7. **子代理起的 dev/preview server 随子代理进程结束被杀**——验证"本地真跑"里程碑时 HTTP 000。主线程需 `nohup npm ... run preview/server &` 重起才存活。→ 手册「把调试现场开给用户看」可提醒：截图在子代理内做完，但要保持 server 存活须主进程 nohup 起。
8. **better-sqlite3 native 编译 + playwright chromium revision mismatch**：@playwright/test 1.61 要的 chromium 版本比之前 `playwright install chromium` 装的新，E2E 前需再 `npx playwright install chromium`。→ 环境预检可加 playwright 版本↔浏览器匹配检查。
9. **E2E strict-mode locator 歧义**：`getByText('Plan less.')` 在 hero `<h1>` 和 footer 都命中 → 断言要 scope 到具体区块。（子代理已处理，作回归锁经验。）
10. **FocusFlow 项目本身未 git init**（子代理留给 ⑧ 处理）——⑧ 部署阶段才 git init。

### 下次怎么起（P3-1 优先，或 ⑧ 部署）
- **P3-1 自定义首图**（改工具本身，见改造需求清单 P3-1）：server `uploadHero`(仿 uploadRef) + `HeroPanel.tsx` 上传入口 + `npm run build` 提交 dist + phase-3 手册加「用户首图→跳过 gen-heroes」分支。**注意这是改 productflow 工具，不是改 FocusFlow**；可用 ~/code/focusflow 做验证场景。
- **⑧ 部署**：需用户给部署目标(VPS/Cloudflare/Node主机)+凭证，停下问。
- **手册改进点**：本轮累积 10 条「改造完善素材」（见 §8+§9），最值钱：step/artifact 收尾易漏 → phase done 加校验。

### task 跟踪（二段）
#6-#12 completed（①→⑦ 全通）；#13 ⑧(需凭证·下轮)；#14 P3-1(下轮优先)；#15 手册改进(10条素材)。

---

## 10. 三段更新（2026-07-06）——P3-1 + 手册改进完成，backlog 仅剩 ⑧

> 本轮把 P3-1 与手册/工具改进都做完了。**用户改造的三条主线全落地**：①→⑦ 跑通验证 + P3-1 功能改造 + 手册工具完善。

### P3-1 自定义首图 done（2.33.0）
`server.py` uploadHero 块 + `pf_state.py` add-hero `--source` + `Canvas.tsx` HeroDialog 上传入口（拖入/粘贴/选文件）+ `phase-3-hero.md` 「source==user→跳过生图」分支 + `test_server.py` uploadHero 测试。上传即定基调、可再生成覆盖。详见改造需求清单 P3-1〔已实施 2.33.0〕。

### 手册 + 工具改进 done（2.34.0）
- **手册补漏 4 处**：phase-3 修 `条镇` 乱码 + set-token alias 示例；phase-2 token 命名分层正反例（✅color.blue.500 / ❌color.primary 放 primitive）；phase-6 图标库引导（lucide/heroicons 不用字符图标）。
- **phase done 收尾软校验**（`pf_state.cmd_phase`）：标 done 时若有 step 未收尾（非 done/skipped）→ stderr 警告（**不 block**），防本轮反复踩的漏标 step 坑；带单测 `test_phase_done_warns_unfinished_steps`。

### backlog 状态（几乎全清）
✅ #6-#12（①→⑦ 全通 + 完整产品本地真跑）｜✅ #14 P3-1｜✅ #15 手册改进｜⬜ **#13 ⑧ 部署（唯一剩，需用户凭证）**。

### 剩余可选素材（未做·非核心，想做再做）
生图 `--count` 中转只出 1 张的手册提醒；子代理起的 server 需主线程 `nohup` 的提醒；better-sqlite3/playwright chromium 版本匹配的环境预检；phase done「未注册产物」校验（比 step 校验复杂——难判哪些磁盘文件该注册，暂缓）。

### 状态
- 全在 **dev 分支未 push**（还原度 5 期 + P3-1 + 手册改进，~40 提交）。
- 完整产品仍在本地：`8787` 完整版 / `4173` 纯前端；`7717` 操作台已到 **2.34.0**。
- 下次：⑧ 部署（给凭证）或收尾 push。
